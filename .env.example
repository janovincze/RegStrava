# RegStrava Environment Configuration

# Server
PORT=8080

# Database (port 5433 to avoid conflicts with existing PostgreSQL)
DATABASE_URL=postgres://regstrava:regstrava@localhost:5433/regstrava?sslmode=disable

# Redis (port 6380 to avoid conflicts with existing Redis)
REDIS_URL=redis://localhost:6380

# Security - CHANGE THESE IN PRODUCTION!
# Generate with: openssl rand -base64 32
HMAC_KEY=your-super-secret-hmac-key-change-in-production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
